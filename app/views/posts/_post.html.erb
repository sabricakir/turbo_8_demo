<div id="<%= dom_id post %>">
  <p class="my-5">
    <strong class="block font-medium mb-1">Title:</strong>
    <%= post.title %>
  </p>

  <p class="my-5">
    <strong class="block font-medium mb-1">Body:</strong>
    <%= post.body %>
  </p>

  <% if action_name != "show" %>
    <div class="flex gap-x-3">
      <%= link_to "Show this post", post, class: "rounded-lg py-3 px-5 bg-sky-100 inline-block font-medium" %>
      <%= link_to "Edit this post", edit_post_path(post), class: "rounded-lg py-3 ml-2 px-5 bg-indigo-100 inline-block font-medium" %>
    </div>
  <% end %>
</div>
<%= button_to "Destroy this post", post_path(post), method: :delete, class: "rounded-lg py-3 mt-2 px-5 bg-red-100 inline-block font-medium" %>
<hr class="mt-6">

